# Prediction microservice (FastAPI) — lean deps for the Docker image.
# Versions pinned to the environment that produced src/prediction/model.joblib so the
# joblib bundle loads without sklearn/xgboost compatibility warnings in production.
fastapi==0.136.1
uvicorn[standard]==0.47.0
psycopg2-binary==2.9.12
python-dotenv==1.2.2
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
joblib==1.5.3
