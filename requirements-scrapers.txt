# Lean dependency set for the scraper cron jobs (GitHub Actions).
# The scrapers (refresh_upcoming, rankings, news) only need these; the heavy ML
# stack (pandas/numpy/scikit-learn/xgboost) in requirements.txt is for the
# prediction model only, so CI installs this instead and starts much faster.
psycopg2-binary
python-dotenv
requests
beautifulsoup4
lxml
feedparser
anthropic
# Chrome TLS-fingerprint impersonation (src/scrapers/http_browser.py): ESPN
# Deportes and ufcespanol.com block plain requests by JA3, not headers.
# Pinned: the impersonation targets are version-sensitive native code.
curl_cffi==0.15.0
